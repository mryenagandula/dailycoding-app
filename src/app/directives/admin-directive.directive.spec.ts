import { AdminDirectiveDirective } from './admin-directive.directive';

describe('AdminDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
